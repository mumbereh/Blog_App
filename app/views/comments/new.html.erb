<h1>New Comment</h1>
<%= form_for([@post, @post.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :text %>
    <%= f.text_area :text %>
  </div>

  <%= f.submit "Create Comment", class: "btn btn-primary" %>
<% end %>
